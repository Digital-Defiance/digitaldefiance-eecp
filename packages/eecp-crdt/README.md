# eecp-crdt

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build eecp-crdt` to build the library.

## Running unit tests

Run `nx test eecp-crdt` to execute the unit tests via [Jest](https://jestjs.io).
