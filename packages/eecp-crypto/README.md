# eecp-crypto

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build eecp-crypto` to build the library.

## Running unit tests

Run `nx test eecp-crypto` to execute the unit tests via [Jest](https://jestjs.io).
