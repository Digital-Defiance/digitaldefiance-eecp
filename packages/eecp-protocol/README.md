# eecp-protocol

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build eecp-protocol` to build the library.

## Running unit tests

Run `nx test eecp-protocol` to execute the unit tests via [Jest](https://jestjs.io).
